# Generate with: openssl rand -hex 32
NUXT_BETTER_AUTH_SECRET=
NUXT_PUBLIC_SITE_URL=http://localhost:3000

# D1 database id (from `wrangler d1 create`). Needed for Cloudflare builds only.
NUXT_HUB_CLOUDFLARE_DATABASE_ID=
